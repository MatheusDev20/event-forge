# Where the experiment write-ups are read from at build time.
# Defaults to ../../experiments (the monorepo folder). Point it somewhere else
# when this app is split out into its own public repository.
EXPERIMENTS_DIR=../../experiments
