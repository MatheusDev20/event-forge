/** @type {import('next').NextConfig} */
const nextConfig = {
  // Nothing here yet — every page is generated at build time from the markdown
  // in ARTICLES_DIR, so the deployed site is static and carries no runtime
  // dependency on the rest of the monorepo.
};

export default nextConfig;
